// Objects in JS are like hashes in other languages
// Keys are valid identifers or strings
// Values can be anything, even arrays or other objects
var pizza = {
    // NOTE: the properties are comma-separated
    // and can be added, removed, or modified at any later time
    toppings: ['cheese', 'sauce', 'pepperoni'],
    crust: 'deep dish', 
    serves: 2
    // NOTE: Don't forget the semicolon after the 
    // var-declaration of a new object
};

console.log(pizza);
