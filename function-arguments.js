// Functions can have any number of arguments of any type
// Returns the result of first + (second * third)
function math (first, second, third) {
    return first + (second * third);
}

console.log(math(53, 61, 67));
